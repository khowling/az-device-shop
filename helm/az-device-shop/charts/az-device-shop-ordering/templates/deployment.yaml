apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "az-device-shop-ordering.fullname" . }}
  labels:
    {{- include "az-device-shop-ordering.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "az-device-shop-ordering.selectorLabels" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
    {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "az-device-shop-ordering.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ default "" .Values.global.registryHost }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: APP_HOST_URL
              value: {{ required "env.APP_HOST_URL Value required" .Values.global.env.APP_HOST_URL | quote }}
            - name: USE_COSMOS
              value: "false"
            - name: STORAGE_ACCOUNT
              value: {{ required "env.STORAGE_ACCOUNT Value required" .Values.global.env.STORAGE_ACCOUNT | quote}}
            - name: STORAGE_CONTAINER
              value: {{ required "env.STORAGE_CONTAINER Value required" .Values.global.env.STORAGE_CONTAINER | quote}}
            - name: STORAGE_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "az-device-shop-ordering.fullname" . }}
                  key: STORAGE_MASTER_KEY
            - name: MONGO_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "az-device-shop-ordering.fullname" . }}
                  key: MONGO_DB
          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
