{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Full Build",
            //"cwd": "${workspaceFolder}/web/web-server",
            "program": "${workspaceFolder}/web-server/server.ts",
            //"outFiles": ["${workspaceFolder}/web-server/out/**/*.js"],
            "preLaunchTask": "build_server",
            "envFile": "${workspaceFolder}/../.env_azure"
        },
        {
            "type": "pwa-node",
            "request": "launch",
            "name": "Launch",
            //"cwd": "${workspaceFolder}/web-server",
            "program": "${workspaceFolder}/web-server/server.ts",
            //"outFiles": ["${workspaceFolder}/web-server/out/*.js"],
            "preLaunchTask": "tsc: build - web-server/tsconfig.json",
            "envFile": "${workspaceFolder}/../.env_azure"
        }
    ]
}